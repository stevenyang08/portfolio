<div class="row">
  <div class="col s12 m8 offset-m2 l6 offset-l3">
    <h2 class="center">Title: <span class="blog-title"><%= @blog.title %></span></h2>
    <p class="center"><%= @blog.created_at.strftime("%B %d, %Y") %></p>
    <%= @blog.content.html_safe %>
  </div>
</div>

<%= link_to 'Back', blogs_path %>